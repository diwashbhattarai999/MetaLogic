import { Metadata } from "next";

export const metadata: Metadata = {
  title: "Services | MetaLogic Software Pvt. Ltd.",
};

const ServicesPage = () => {
  return (
    <div className="min-h-screen flex items-center justify-center text-6xl">
      Services Page
    </div>
  );
};

export default ServicesPage;
