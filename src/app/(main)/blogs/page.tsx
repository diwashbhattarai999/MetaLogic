import { Metadata } from "next";

export const metadata: Metadata = {
  title: "Blogs | MetaLogic Software Pvt. Ltd.",
};

const BlogsPage = () => {
  return (
    <div className="min-h-screen flex items-center justify-center text-6xl">
      Blogs Page
    </div>
  );
};

export default BlogsPage;
